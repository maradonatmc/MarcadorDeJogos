@model MarcadorDeJogos.Models.Jogadores

@{
    ViewData["Title"] = "Detalhes";
}

<h1>@ViewData["Title"]</h1>

<div>
    <h4>Jogadores</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-10">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
        <dt class="col-sm-10">
            @Html.DisplayNameFor(model => model.DatCadastro)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DatCadastro)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@Model.Id">Editar</a>
    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@Model.Id">Excluir</a>
    <a asp-action="Visualizar">Voltar</a>
</div>